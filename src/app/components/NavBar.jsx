"use client";
import Link from "next/link";

const sections = ["home", "about", "work", "skills", "projects", "playground", "contact"];

export default function Navbar() {
  return (
    // <nav className="fixed top-0 right-0 z-50 p-4">
    //   {/* <ul className="flex space-x-6 text-sm text-foreground font-body">
    //     {sections.map((sec) => (
    //       <li key={sec}>
    //         <a href={`#${sec}`} className="hover:text-gold transition">
    //           {sec.charAt(0).toUpperCase() + sec.slice(1)}
    //         </a>
    //       </li>
    //     ))}
    //   </ul> */}
    // </nav>
    <></>
  );
}